@kind(vocabulary)
@vocabulary(bookmark){ version: "1.0.0" }

@element(bookmark){
  display: block
  @args{
    @param(url){ type: string, positional: true, required: true }[ 保存する URL。 ]
  }
}[
  ブックマークひとつ。
  `docs/examples/bookmark.tmt` が使い方の見本。
]
