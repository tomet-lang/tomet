# serde_tomet

Tomet のデータサブセット（`tomet_ast::Value`）に対する Serde シリアライザ・デシリアライザを提供するクレート。

## 責務と機能

-. **データサブセットのデシリアライズ (`from_str`)**:
   - データ専用の `.tmt` テキスト（マップ、配列、スカラー）を Rust の構造体・Enum へ変換。

-. **データサブセットのシリアライズ (`to_string`)**:
   - `serde::Serialize` を実装した任意の Rust データ型を `.tmt` 形式の文字列へシリアライズ。
