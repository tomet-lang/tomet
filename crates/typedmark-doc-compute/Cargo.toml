[package]
name = "typedmark-compute"
version.workspace = true
edition.workspace = true

[dependencies]
typedmark-ast = { workspace = true }
typedmark-resolver = { workspace = true }

[dev-dependencies]
typedmark-parser = { workspace = true }
