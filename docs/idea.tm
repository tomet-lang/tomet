- <T()>はどう？
- --advancedでserveした場合に、色々試せるように、設定をGUI経由でできるようにする（html出力はそのままで。）
  - urlを何らかの方法で見えるようにする。
- <var>(boolean){ bool1:true, }
- ${var} or {{var}}
  - これ、もしかして共通記法じゃないか？
  - $と{}で。しかし共通の場合、$()じゃないか？
- アウトライン記法をもっともっと練りたい。
- @tag(unko)
  - @{unko}省略できるのでは？
- <index>(){}
