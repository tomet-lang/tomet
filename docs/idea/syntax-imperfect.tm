
- `[]`にkeyが設定できない。<T>がそもそもkeyになる？？いや。複数回使用できてしまっている。
- `(args)`内のrequired要件が、これがあるならこっちは宣言できない（しなくてよい）みたいな依存関係が生まれている。
  - 実際には、起きているのは決められた２つだけ。これらを一つのenumとして考えれば良いかも？
  - `target` -> `path`, `file`, `ref`
  - `id` -> `id`, `ids=[]`
