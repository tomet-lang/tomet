// `${...}`の構文自体はtomet-parserに実装済み。
// ${name(args)}はInterpExpr::Callとしてパースされるが、"name"が実際に何をするか(関数の一覧・
// 挙動)を決める評価/レジストリはまだ存在しない -- tomet-computeの未実装分。
// docs/design/ideas/idea.tmt の「必要か？な関数」節にある構想メモ($regex, $add, $math, $summary)は
// まだ候補段階。
//
// 識別子には `-` を含められる。要素の引数と同じ規則なので、`node-graph` のような
// kind をそのまま `${ref(id(node-graph))}` と書ける。

${sum(1, 4)}
${ref(id(asdf-asdfa).contents(dafault))}
$uuid()
$date()
$time()
$regex("/*.svg/g")
${filename}
${title}

// 以下は未実装:
//
// ${vars.*}    // ワイルドカードのメンバアクセス。`.` の後には識別子しか来られない
