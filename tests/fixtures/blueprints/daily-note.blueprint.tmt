@settings(file:docs/docs.settings.tmt)
@kind(blueprint)
@blueprint(daily-note)
@meta{
  uuid: ${uuid}
}

=[ TODO ] {id:todo}

=[ Review ] {id:review}

=[ Feadback ] {id:feadback}

=[ Timeline ] {id:timeline}
