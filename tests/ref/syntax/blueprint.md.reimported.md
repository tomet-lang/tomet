# 三つのスロット

識別子は `blueprint`、入力は生成物の kind、データは雛形自身についての情報。 位置引数は `target` に正規化される。`kind` ではない——すぐ上の ``@`kind` と 二行のうちに二度出て、別のものを指すことになるので。

# 生成物ではない

このファイルは memo ではなく、memo の定義。``@`kind()` で memo を探しても ここは出てこない。実体化のとき ``@`blueprint(memo)` が ``@`kind(memo)` に昇格する。

# 構造の要求

# Plan

# Review

