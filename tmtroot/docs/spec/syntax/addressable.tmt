全ての要素、全ての値にアクセス可能な、アドレスを公開する。

=[ 外部のアドレス ]

- ファイルパス
- URL(Web)

=[ 絶対アドレス ]

たとえ、識別できるようなプロパティが宣言されていなかったとしても、ドキュメントのルートからアドレスが通る。

==[ 位置による番地 ]

`list()`など。

=[ 一意なアドレス ]

`{}`などで宣言された、ユニークプロパティを使用したアドレス表現。

==[ 参照 ]

`^key(value)`で、参照を可能にする。

=[ 使用 ]

==[ 参照保証性 ]

構造が変わった際の耐久性。
